Anel de conclusão do morador no período — disco de tinta sobre papel com número de cartaz (Anton) no centro.

```jsx
<ProgressRing value={54} tone="barro" />
<ProgressRing value={0} size={80} tone="ocre" />
```

Props: `value` (0–100), `size` (px), `tone` (barro/verde/indigo/ocre).
